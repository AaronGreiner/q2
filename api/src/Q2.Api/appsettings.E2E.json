{
  "// Purpose": "End-to-end runs. Playwright creates a uniquely named temporary SQLite file, migrates and seeds it before the server starts, and deletes it afterwards.",
  "ConnectionStrings": {
    "Database": "Data Source=q2-e2e.db"
  },
  "Database": {
    "MigrateOnStartup": true,
    "SeedOnStartup": false,
    "ResetOnStartup": false,
    "SeedProfile": "E2E",
    "AllowDestructiveReset": true
  },
  "Sentry": {
    "SampleRate": 1.0,
    "TracesSampleRate": 1.0
  },
  "Q2": {
    "PublicAppUrl": "http://localhost:3000",
    "// Mail": "Nothing is sent: every mail becomes a .eml file. Playwright points FileDirectory and PublicAppUrl at its own run (tests/e2e/support/e2eEnvironment.ts) and reads the reset link out of the mail.",
    "Mail": {
      "Transport": "File",
      "From": "noreply@q2.invalid"
    }
  },
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:3000"
    ]
  }
}
