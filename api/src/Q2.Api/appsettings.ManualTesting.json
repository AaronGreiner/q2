{
  "// Purpose": "Exploratory manual testing. DESTRUCTIVE BY DESIGN: every start deletes the database, reapplies all migrations and inserts the ManualTesting seed, so the starting state is always identical.",
  "Urls": "http://localhost:5080",
  "ConnectionStrings": {
    "Database": "Data Source=q2-manual-testing.db"
  },
  "Database": {
    "MigrateOnStartup": true,
    "SeedOnStartup": false,
    "ResetOnStartup": true,
    "SeedProfile": "ManualTesting",
    "AllowDestructiveReset": true
  },
  "Sentry": {
    "TracesSampleRate": 1.0
  },
  "Q2": {
    "PublicAppUrl": "http://localhost:3000",
    "// Mail": "Nothing is sent: every mail becomes a .eml under api/.data/mail, which any mail client opens. The API logs where it wrote each one.",
    "Mail": {
      "Transport": "File",
      "From": "noreply@q2.invalid"
    }
  },
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:3000"
    ]
  }
}
